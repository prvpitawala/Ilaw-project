/**
 * UI Management System Constants
 * Defines constants used throughout the UI system
 */

// Component stage constants for better readability
export const STAGES = {
    HIDDEN: '--hidden',
    PRIMARY: '--primary',
    SECONDARY: '--secondary'
  };

// Event groups 
export const EVENTGROUPS = new Set(['register','dashboard','message','setting','collectionUpload','fileUpload']);
